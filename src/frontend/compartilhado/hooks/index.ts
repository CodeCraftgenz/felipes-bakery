/**
 * Barrel de Hooks — Felipe's Bakery
 *
 * Ponto de importação único para todos os hooks customizados.
 *
 * @example
 * import { useAuth, useDebounce } from '@frontend/compartilhado/hooks'
 */

export { useAuth }         from './useAuth'
export { useDebounce }     from './useDebounce'
export { useLocalStorage } from './useLocalStorage'
